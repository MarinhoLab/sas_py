name: Build and Publish

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm, windows-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Init submodules
        run: |
          git submodule update --init --recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      # ── Ubuntu dependencies ─────────────────────────────────────
      # libdqrobotics comes from the DQ Robotics PPA (shared library —
      # see "dqrobotics is dynamically linked on every platform" in
      # CMakeLists.txt); patchelf is a native prerequisite of
      # `auditwheel repair` below, which bundles it into the wheel.
      - name: Install system dependencies (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential patchelf
          sudo apt-get install -y libeigen3-dev
          sudo add-apt-repository -y ppa:dqrobotics-dev/development
          sudo apt-get update
          sudo apt-get install -y libdqrobotics
          # dqrobotics headers use <Eigen/Dense> without the eigen3/ prefix:
          sudo ln -s /usr/include/eigen3/Eigen /usr/include/Eigen

      # ── macOS dependencies ──────────────────────────────────────
      # (macOS is not in the matrix yet; kept for when it is added.)
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install eigen
          git clone --depth 1 https://github.com/dqrobotics/cpp.git dqrobotics/cpp
          cmake -S dqrobotics/cpp -B dqrobotics/cpp/build \
                -DCMAKE_BUILD_TYPE=Release \
                -DCMAKE_PREFIX_PATH="$(brew --prefix)"
          cmake --build dqrobotics/cpp/build -j
          cmake --install dqrobotics/cpp/build --prefix /usr/local

      # ── Windows dependencies ────────────────────────────────────
      # dqrobotics/cpp expects the vcpkg toolchain at C:/vcpkg.
      # C:/vcpkg is pre-installed on windows-latest, so clone only if
      # absent (the old unconditional clone failed every run).
      - name: Prepare vcpkg at C:/vcpkg
        if: runner.os == 'Windows'
        run: |
          if not exist C:\vcpkg\bootstrap-vcpkg.bat (
            git clone https://github.com/microsoft/vcpkg C:\vcpkg
          )
          C:\vcpkg\bootstrap-vcpkg.bat
          C:\vcpkg\vcpkg install eigen3:x64-windows

      - name: Build & install dqrobotics (Windows)
        if: runner.os == 'Windows'
        # '^' line-continuation is cmd.exe syntax (the default runner shell
        # is pwsh, where it would be passed literally to cmake).
        shell: cmd
        run: |
          :: Use the DEFAULT (Visual Studio / MSVC) generator so dqrobotics is
          :: built with cl.exe, the same compiler the wheel uses. Do NOT use
          :: -G Ninja here: on this runner Ninja picks the pre-installed MinGW
          :: g++ and produces a libdqrobotics.dll.a that MSVC cannot link.
          git clone --depth 1 https://github.com/dqrobotics/cpp.git dqrobotics\cpp
          cmake -S dqrobotics\cpp -B dqrobotics\cpp\build ^
                -DCMAKE_TOOLCHAIN_FILE=C:\vcpkg\scripts\buildsystems\vcpkg.cmake ^
                -DVCPKG_TARGET_TRIPLET=x64-windows ^
                -DCMAKE_INSTALL_PREFIX=C:\dqrobotics
          cmake --build dqrobotics\cpp\build --config Release -j
          cmake --install dqrobotics\cpp\build --config Release
          :: The install layout is fixed by dqrobotics/cpp's install() rules:
          ::   C:\dqrobotics\lib\dqrobotics.lib   (import library, for the link)
          ::   C:\dqrobotics\bin\dqrobotics.dll   (runtime, bundled into the
          ::                                       wheel by the POST_BUILD copy
          ::                                       in the top-level CMakeLists.txt)
          :: No copy of the .lib is needed: sas_cpp's find_library searches
          :: C:/dqrobotics (CMAKE_PREFIX_PATH below) and finds it in lib\.

      # ── Build ───────────────────────────────────────────────────
      # On Windows, point sas_cpp's dqrobotics lookup at C:/dqrobotics.
      # setup.py forwards CMAKE_ARGS to the CMake configure step; the
      # top-level CMakeLists.txt uses the same CMAKE_PREFIX_PATH to locate
      # and bundle dqrobotics.dll next to _core.pyd.
      - name: Build wheel
        run: |
          python -m pip install --upgrade pip
          pip install build
          python -m build --wheel
        env:
          CMAKE_ARGS: ${{ runner.os == 'Windows' && '-DCMAKE_PREFIX_PATH=C:/dqrobotics' || '' }}

      # PyPI rejects wheels tagged `linux_x86_64`/`linux_aarch64` (it requires
      # a PEP 600 `manylinux` tag). auditwheel relinks/bundles as needed and
      # retags the wheel.
      # - `--plat auto` picks the tightest compatible manylinux tag from the
      #   wheel's actual symbols (the ubuntu-latest glibc varies, so do not
      #   hardcode a `manylinux_2_X` tag).
      # - repair leaves the original wheel in dist/, so the `linux_*` wheel
      #   is removed before upload (it would 400 on PyPI).
      - name: Repair wheel (Linux, manylinux tag)
        if: runner.os == 'Linux'
        run: |
          pip install auditwheel
          arch=$([[ "${{ matrix.os }}" == "ubuntu-latest" ]] && echo x86_64 || echo aarch64)
          auditwheel repair --plat auto -w dist/ dist/*linux_${arch}.whl
          rm -f dist/*linux_${arch}.whl

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v6
        with:
          name: wheel-${{ runner.os }}-py${{ matrix.python-version }}
          path: dist/
          retention-days: 7

  publish:
    needs: [build]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    permissions:
      id-token: write

    steps:
      - name: Download all wheel artifacts
        uses: actions/download-artifact@v6
        with:
          pattern: wheel-*
          merge-multiple: true
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
